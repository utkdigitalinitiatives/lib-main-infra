# Copy to terraform.tfvars (gitignored) and fill in real values.

subscription_id = "9fc717ef-b8c4-4858-a677-7c7b518616a0"

# Object ID of the lib-main-github-actions SP. NOT the App/Client ID.
# Fetch: az ad sp show --id 3e3f4430-45ef-4206-81d0-0c912fc5e158 --query id -o tsv
gh_actions_sp_object_id = "REPLACE_WITH_SP_OBJECT_ID"

# Object IDs of human operators that need full secret read/write.
# Fetch your own: az ad signed-in-user show --query id -o tsv
operator_object_ids = [
  # "REPLACE_WITH_YOUR_OBJECT_ID",
]
